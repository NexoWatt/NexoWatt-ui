NexoWatt UI 0.8.215 RC90 – SSE Stability + Publish-Manifest-Fix

Veröffentlichen ausschließlich aus diesem frisch entpackten Repository-ZIP:

npm publish

Der Prepublish-Guard prüft automatisch:
- ob Version 0.8.215 auf npm noch frei ist,
- ob alle 310 npm-Paketdateien exakt dem versiegelten Release-Artefakt entsprechen,
- ob die Version in package.json und io-package.json synchron ist.

RC90 korrigiert den LIVE-/SSE-Resync und verhindert die zuvor beobachtete
Reconnect-/resync-error-Schleife. Der Publish-Fix verändert keine Runtime- oder
Regellogik. Er versiegelt lediglich das Release-Artefakt nach sämtlichen RC90-
Änderungen neu, damit io-package.json, main.js, die betroffenen Prüfscripte und
www/manifest.webmanifest mit ihren tatsächlich ausgelieferten Inhalten geprüft
werden.

Wichtig:
- Den alten RC90-Ordner vollständig löschen.
- Dieses ZIP in einen neuen, leeren Ordner entpacken.
- Keine Dateien aus dem alten Ordner hinein kopieren.
