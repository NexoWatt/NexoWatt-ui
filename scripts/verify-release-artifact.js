"use strict"; const fs=require("fs"),path=require("path"),crypto=require("crypto"),root=path.resolve(__dirname,".."),m=require("./release-artifact-manifest.json"),pkg=require("../package.json"); const sha=f=>crypto.createHash("sha256").update(fs.readFileSync(f)).digest("hex"); if(pkg.name!==m.package||pkg.version!==m.version){console.error("[release-artifact] ERROR: version mismatch");process.exit(1);} let n=0; for(const [r,h] of Object.entries(m.files)){const f=path.join(root,r);if(!fs.existsSync(f)||sha(f)!==h){console.error(`[release-artifact] ERROR: ${r} fehlt oder wurde verändert.`);process.exit(1);}n++;} console.log(`[release-artifact] OK: ${pkg.name}@${pkg.version}, ${n} Dateien unverändert; read-only.`);
