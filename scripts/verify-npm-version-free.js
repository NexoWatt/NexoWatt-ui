"use strict"; const https=require("https"),pkg=require("../package.json"); const u=`https://registry.npmjs.org/${encodeURIComponent(pkg.name)}/${encodeURIComponent(pkg.version)}`; const r=https.get(u,{headers:{"User-Agent":`NexoWatt-EOS-release/${pkg.version}`}},res=>{res.resume(); if(res.statusCode===404){console.log(`[npm-version-free] OK: ${pkg.name}@${pkg.version} ist frei.`);return;} if(res.statusCode>=200&&res.statusCode<300){console.error(`[npm-version-free] ERROR: ${pkg.name}@${pkg.version} existiert bereits.`);process.exitCode=1;return;} console.error(`[npm-version-free] ERROR: Registry HTTP ${res.statusCode}; fail-closed.`);process.exitCode=1;}); r.setTimeout(15000,()=>r.destroy(new Error("timeout"))); r.on("error",e=>{console.error(`[npm-version-free] ERROR: ${e.message}`);process.exitCode=1;});
